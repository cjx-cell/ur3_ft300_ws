# 已退役的 SA-MoE：唯一历史说明

2026-09-09 起，SA-MoE 不再作为本项目可运行策略发布。此前仓库主线是 Pi0 抓放与 SA-MoE 轴孔插入，旧版本使用阶段监督、StageGate 和五专家等配置；不同历史版本还有 Top-K、阶段平衡、LoRA 等变化，不能将某一版本定义套到全部历史代码。

当前 PAP-MoE 基于 Pi0.5：四个专家分别面向正常视觉自由运动、视觉退化、刚性接触、可运动/顺应接触；PhysicsGate 预测 50 步三因子并导出逐步软路由，特征在动作 Transformer 之前融合。当前不使用 subtask/progress 头，不继续运行旧 `sa_moe_pi0` 训练。

删除范围包括旧 SA-MoE 策略模块/注册、独立推理/评估脚本、旧训练入口和旧配置。与 PAP 共用的 ROS/数据/控制代码不能仅因历史注释出现 samoe 就删除。PAP 配置中为旧 PAP 检查点保留的少量 `sa_moe_*` 字段别名不是旧模型实现。

公开 Git 历史保留原版本；本地移除内容可由 `/home/ubuntu/ur3_ft300_retired/20260909/` 及整理清单恢复，不放回当前项目入口。冻结实验快照中的旧注册依赖暂随证据保留且不发布，不能修改快照破坏数值复现。历史 SA-MoE 文档不再作为当前操作指南。
