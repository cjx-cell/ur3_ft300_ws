# 原S4成功组合：配对闭环与同观察动作分叉

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent（inline）
- Origin Mode: run；本地仿真与保存观测，无训练、无上传。
- Origin Date: 2026-09-09
- Verification Status: 历史S4前4块完整RTC重建差值0，监督流程已启动；最终结果待运行。

## 固定对象与执行合同

四个位置—seed组合：0001/1、0031/2、0041/1、0041/2，来自原S4成功记录，不重新挑选。
四个检查点：原S4/015000、上一轮融合B/010000、本轮GT/010000、本轮Pred/010000，共16条。
统一修复后的fresh-read夹爪插件、50预测/10执行、arm-only EXP RTC max10 delay0、0.13rad手臂限制、120仿真秒及原成功规则。
每模型常驻，每条Gazebo冷启动，录制视频、逐块观测/动作与控制反馈。失败照常计入，不静默重跑，不放宽成功标准。

这是按历史成功挑选的回归诊断集合，不能当作无偏总体成功率比较。旧S4在旧插件下的4次成功不混入本轮成绩。S4→B包含融合改变与联合训练，B→GT/Pred才是固定融合后的续训分支。

## 同观察完整Flow

1. 参考集合固定为原4条成功S4轨迹＋本轮4条S4轨迹，不论本轮是否成功。
2. 每条参考轨迹全部保存观测都回放给四个模型，使用同噪声、同原始观察历史。
3. RTC组固定使用参考S4上一块归一化动作尾部；无RTC组关闭引导。避免候选各自的不同历史动作干扰映射比较。
4. 分别记录首次数值差异（>1e-5rad）及首次前10步任一臂关节差>0.01rad或夹爪差>0.02rad的块。仅诊断阈值，不是控制阈值，也不是动作错误的真值判据。
5. 分别比较S4→B、B→GT、B→Pred；保存所有块的完整50步动作，不止首次分叉点。S4自身回放需通过记录动作误差<1e-5rad校验，同观察预测路由及噪声另做逐值检查。

若第0块就有分叉，应如实报告；不能把“动作不同”直接描述为“从此已经失败”。对各自闭环按块比较仅提供时间线，不算同观察因果证据。原始动作不是监督真值。

## 监督与材料

入口：`artifacts/deployment_validation_20260909_s4_success_lineage/run_pipeline.py`。
总体：同目录`status.json`；闭环：`closed/status.json`；回放：`replay_S4/`、`replay_B/`、`replay_GT/`、`replay_Pred/`。
闭环逐条核验实际加载插件（含seed的16个唯一键）、源码、检查点身份与结果有效性；记录进程、心跳和超时。工程异常停下，不隐式重试。原暂停调度857240不得恢复。
最终自动生成：`docs/reports/PAP_MoE_S4成功组合配对与动作分叉_20260909.md`。
默认模型代码和插件不修改；复用上一轮只读统一代码。既有数据与检查点全部保留。
