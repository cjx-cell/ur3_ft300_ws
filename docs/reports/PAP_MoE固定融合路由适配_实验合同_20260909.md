# 固定融合接口：真实路由与预测路由联合适配

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent（inline）
- Origin Mode: run；本地实验，无外传。
- Origin Date: 2026-09-09
- Verification Status: 接口预检通过，正式训练已启动，结果待评估。

## 唯一变量

两组都从上一轮B连续融合接口的010000检查点开始，固定log(route)注意力先验、K/V路由缩放和动作Transformer前融合位置。GT用真实50步路由；Pred用冻结PhysicsGate根据当前观察预测的50步路由。不是训练Gate，也不是只训练小型动作头。

共同更新四物理专家、既有辅助表征/历史模块、融合层和完整动作专家；VLM、PhysicsGate及共享力/本体/质量编码器冻结。数据仍50条14418帧，processor统计不变；原增强、采样、动作Flow损失、表征和记忆辅助损失不变。没有基线动作保持损失、负载均衡损失或新恢复数据。

各10000步，batch1，seed1000，两优化器组峰值LR均2.5e-5，5000/10000保存。scheduler与上轮一致，配置1000/30000在10000步预算下实际缩放为333/10000。不根据闭环结果择优挑选中间检查点，主评估固定010000。

## 预检与监督

- 旧train_expert_action_joint强制真实路由，单改teacher-forcing参数无效；隔离代码新增训练专用expert_action_training_route选项，不新增网络参数、不改部署默认路由。
- 实际训练forward与完整推理在相同观察上的Gate路由逐值一致，GT/Pred各4锚点检查通过。预检中的180帧实际是接近对准，非运输标签；不拿它作分阶段统计。
- 首次预检漏传测试标签字段，被拒绝且未执行优化器步骤；失败记录保留。v2补齐与训练一致的50步标签后通过。
- 训练每个batch断言实际路由来源及冻结Gate无梯度；首3步及每1000步保存观察/标签/预测路由指纹。两组最终比较采样与条件指纹，核对未因分支改变随机序列。
- 各检查点审核权重更新及归一化不变。监督进程记录PID、命令、30秒心跳、日志与硬超时；出错停止，不静默重试。

## 自动执行顺序与位置

GT训练→5000/10000权重审计→480次完整Flow→120次接触补测；Pred同样流程；之后两组配对开环审计、各5位置seed0闭环、执行链/释放质量审计、自动生成结果报告。

闭环固定50预测/10执行、arm-only RTC、上一轮候选夹爪插件及原成功判据，模型每组常驻、Gazebo每条重启并录制视频。仅对比GT/Pred共10条；上一轮Pi0.5的1/5只能作同执行版本的背景参考，不混入本轮计分。

入口：`artifacts/deployment_validation_20260909_route_adaptation/run_pipeline.py`。
状态与日志：同目录`status.json`、`GT_train.log`、`Pred_train.log`。
检查点：`outputs/train/pap_route_adaptation_20260909_GT/`、`outputs/train/pap_route_adaptation_20260909_Pred/`。
最终报告预定：`docs/reports/PAP_MoE固定融合_真实与预测路由联合适配_20260909.md`，仅所有审计通过后生成。

尚不能报告训练效果或成功率。候选源码隔离保存，未修改默认安装模型与插件。旧暂停调度857240不要恢复。
