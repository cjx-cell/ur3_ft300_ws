## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-08
- Verification Status: UNVERIFIED
- Version Label: joint_atomic_v2_comparison

# 新旧执行版本差异与统一对比

## 结论先行

修复的是ROS关节观测采集的并发一致性，不是重新训练模型、改变PAP结构或再次调整归一化。旧结果保留；新版本必须让Pi0.5、PAP阶段3/4各自完整重测15次，不能把修复后的成功补进旧版失败，得到混版本成功率。

现阶段对比的是同执行版本下的普通插孔能力，**不是同信息输入的MoE结构消融**。PAP有FT和历史条件，现有Pi0.5没有等价物理信息融合；训练预算也不同。本轮不能单独证明四专家结构、密集接触安全性或视野丢失恢复的优势。

## 1. 到底改了什么

指纹来源：旧`paired_comparison_20260908_controller013_v1/runtime_sources.json`与补测`joint_history_followup_20260908_v1/runtime_sources.json`。共同文件中只有以下两份哈希不同：

- `ur3_peg_in_hole_ros_side_base.py`：旧`4fc378e8…e2e96e`，新`e1484dc9…2985d`。
- `ur3_pap_moe_peg_in_hole_ros_side.py`：旧`380c2ed9…130f`，新`29aaf056…a84d3`。

| 项目 | 旧执行版本 | 修复后版本 |
|---|---|---|
| 关节回调 | Reentrant，两个回调可能交错 | 独立MutuallyExclusive回调组；图像/力不随其串行 |
| 当前状态与历史 | 基类更新当前值，子类重新加锁读取latest_pos | 同一锁内更新当前值、源时间、该消息对应历史 |
| 竞态后果 | 新消息先完成时，旧消息可清空历史，并把新值配给旧时间 | 消除已复现的两段锁间交错窗口 |
| 真时间回退 | 清空历史，缺少定位日志 | 仍清空，并记录stamp、previous、sim_now，不伪造有效样本 |
| 历史长度/超时阈值 | 既有配置 | 不变，没有降到6帧或放宽动作等待 |
| 模型架构/权重/统计/路由 | 现有有效检查点 | 不变，新启动器重新记录所有检查点文件SHA256 |
| 50/10、RTC、0.13rad、连续夹爪、无attach | 旧批既有合同 | 保持，不更换保护阈值 |
| 终止与评分 | 旧插入终止＋几何影子v2 | 保持 |

旧竞态的确定性线程交错已复现，生产原子更新/互斥配置/真回退用例通过。启动前轻量回归146项通过。这不等于证明原始异常的唯一原因，也不等于全部工程风险清零。

## 2. 已有结果，按版本隔离

| 版本/范围 | Pi0.5 | PAP阶段3 | PAP阶段4 |
|---|---:|---:|---:|
| 旧正式批，有效试验 | 5/15 | 0/15 | 1/11 |
| 修复后0031/seed2，配对工程测试 | 0/1 | 未测 | 1/1 |
| 修复后0041三个seed，补充工程测试 | 未测 | 未测 | 2/3 |

旧正式批另有1条工程无效（S4/0031/seed2，历史不足6帧、paired等待超时），随后批次停止，不能称45次全部完成。旧汇总中5条unknown_failure，经日志核对均为控制器目标超时，补充原因见工程审计第14节，旧JSON不覆盖。

修复后S4成功样本：0031/seed2约79.97mm；0041/seed0约80.01mm；0041/seed1约76.03mm、未到底。它们是积极局部证据，但这些位置是沿此前故障和缺测选定的，不代表随机无偏的新全集，不能直接宣布整体提升。所有成功均未验证释放后稳定。

## 3. 新版统一45次合同

- 批次目录：`artifacts/paired_comparison_20260908_joint_atomic_v2/`。
- 三模型：Pi0.5 `pi05_workspace50_global_stats_expert_only_30k_20260827/030000`；S3 `pap_corrected_physicsgate_sequence_20260907_091425/015000`；S4 `pap_corrected_gate_calibration_20260907_104300/015000`，完整路径在新批contract/checkpoint_manifest中。
- 每组：0001/0011/0021/0031/0041，各seed0/1/2；每模型15次，总计45次，从空结果表开始。
- 预测50、执行10；RTC开启；每步0.1s；机械臂上限0.13rad；120s仿真时长上限；goal宽限继承5s。连续绝对夹爪，不attach，不加手写恢复。
- 主终止仍为旧插入阈值，影子v2分别记录曾观测到有效插入/几何到底。旧终止可能提前截断下插，未观测到底不等于模型永远不能到底；单帧达到也不等于释放后稳定。
- 运行顺序：模型→位置→seed，与旧批一致；不是随机交错。所有模型共享版本并不消除时间漂移/仿真非确定性，三个seed也不代表三个新工作空间。
- seed0录视频，所有试验保留动作/几何诊断。任何工程无效停止且保留，不自动重试或删除。
- 检查点启动前全文件SHA256；每次运行前检查源代码与批快照一致、检查点大小/mtime未变，运行后检查当次源代码。不得运行中编辑冻结源文件。

现有启动器`contract.id`沿用控制/几何合同名`workspace50_20260908_controller013_geometryv2`；**执行版本的唯一批次身份必须同时使用新目录名和源码指纹**，不能只凭这个沿用的字段认定两批代码相同。本轮不为改字段热改已验证启动代码。

## 4. 状态与后续汇总口径

当前状态：新版统一批已启动，尚未完成，不填造最终成功率。查看`batch.log`、逐条`results.jsonl`；全部正常结束才生成`results.json`、`summary.json`及`completion.json`。异常停止时completion会标false。

17:44启动校验：新旧checkpoint_manifest均为24个文件，所有SHA256逐文件一致；新批runtime_sources与修复后成功补测的runtime_sources完全一致。首条Pi0.5/0001/seed0已启动，产物`gazebo_pi05_workspace50_20260908_174430_ep0001_seed0`。此为启动快照，不是最终进度。

最终按模型×位置同时报告成功数、有效次数、工程无效次数、控制器终止原因、插入/到底几何诊断；保留每个seed与视频对应关系。新旧结果分表；仅作描述性比较，不在15次/5个训练位置上夸大泛化或统计显著性。

尚未消失的限制：载荷/外力标定风险、RTC实际执行时序、夹爪物理负例与动态释放稳定、E2连续遮挡覆盖、同信息无MoE基线。这些不妨碍在明确边界下测普通插入现状，但不能被本次“同版本”措辞掩盖。

## 4. 评估提速候选：模型常驻，Gazebo仍逐条重启

用户已同意开始实现，但要求不影响公平对比。独立实现位于`scripts/resident_policy/`，原正式启动器、模型源码、ROS执行及判定均未改动；重新计算当前冻结运行源码SHA256，变化为0。检查时新版批已完成7/45且7条均有效，非最终结果。

已实现离线常驻worker、episode token/序号隔离、异常废弃会话、每条重置processors/视觉历史/RTC剩余动作/随机种子，并复现原脚本的预热历史。5项会话逻辑测试通过、语法编译通过；这不等于真实模型数值和在线闭环等价。

自动等待程序已启动：`scripts/resident_policy/after_batch.py`，状态在`artifacts/resident_policy_validation_20260908_v1/status.json`。等当前45次全部有效结束并释放GPU，再顺序验证Pi0.5、S3、S4：冷启动A对照常驻A→B→A，比较完整50步输出、前10步、PAP路由，至少覆盖第二块RTC。PAP还对照原在线脚本保存的动作；baseline当前批未保存模型逐块输入，尚需另补原main独立数值参考。若批次异常、源/权重变化、GPU被占用或数值不符则停止，不自动重试。

**当前没有启用常驻正式评估，也没有实测提速。** 数值验证之后还要完成在线paired-I/O适配和同场景冷/热配对闭环验收；只有都通过才统一切换三模型的后续正式评估。不在本轮45次中途切换、不混入候选结果。细节见`scripts/resident_policy/README.md`。

## 5. 常驻模式验证进展（覆盖第4节旧状态）

按用户纠正，已在Pi0.5/0031/seed1结束后暂停后续正式试验，并非等待45条结束。调度shell PID857240处于STOP状态，当前条result正常保存，待恢复shell时再由原程序归并，勿手工重复追加。

离线验证目录`artifacts/resident_policy_after_trial_20260908_v1/`：

| 模型 | 前两块冷/热、A→B→A最大输出差异 | 对照原在线记录 | 后续条重置/预热耗时 |
|---|---:|---|---:|
| Pi0.5 | 0 | 待在线验收补原main参考 | 0.73–0.78s |
| PAP S3 | 0 | 动作、路由均0差异 | 0.81–0.86s |
| PAP S4 | 0 | 动作、路由均0差异 | 0.81–0.82s |

本次完整模型加载约113–227s；这说明省去重复加载有潜力，不是整条Gazebo闭环的已测加速比。仅覆盖两个动作块，含第二块RTC，不等于所有轨迹在线等价。

在线实现已完成：独立模型worker经Unix socket提供串行会话；Gazebo每条启动bridge，使用原观测loader与paired-v1回复；断开连接清理本条会话，模型不重载。6项轻量测试通过。未改变原模型源码、控制器、50/10、RTC、归一化或成功判据。

首条在线试跑`resident_online_canary_20260908_v1/s4_cold`被判工程无效：运行时修改了被记录指纹的`run_canary.py`（补baseline诊断参数），属本次执行组织错误；原模型/ROS代码未变。其本身还触发腕关节训练包络停止，不能删除记录或按成功处理。流水线已停止，未混入正式成绩。

修正措施是先完成代码，再复制为只读快照后启动新一版`artifacts/resident_online_canary_20260908_v2_frozen/`。快照及SHA256在`code/`、`code_manifest.json`。验收顺序S4、Pi0.5、S3，每组：原版冷启动闭环、原main相同观测的socket动作核对、同一常驻worker下两次新Gazebo闭环。冷Pi0.5仅加存档诊断，不加控制模块。有效任务失败保留，不重试；工程无效/通信或数值差异则停止。全部验收前不恢复或切换正式45次批。闭环成功率或耗时尚待结果，不填造。

在线验收首组节点：`v2_frozen/runs/s4_summary.json`已生成。S4/0031/seed2的原版冷启动为有效timeout；相同观测经socket回放前两块，归一化50步、物理50步、执行10步、路由最大差异均0。随后两条Gazebo重启均复用worker PID1104178，session token不同，序号从0重新开始：第一条有效out_of_distribution（17块），第二条有效success（16块），后者几何插入80.003mm且观测到fully_seated，未验证释放稳定。条前重置分别0.863/0.854s；桥接全链路最大延迟304.7/296.7ms，无paired超时或工程无效。成功视频`artifacts/gazebo_pap_moe_workspace50_20260908_195140_ep0031_seed2/pap_moe_pure_rollout.mp4`。

这证明候选通信链路和跨Gazebo模型复用已实际工作，**不证明成功率等价或提升**：冷/热试验轨迹与终止时刻不同，不用399s对69/67s直接计算整条加速比；一组单位置结果也不能排除时序/仿真随机性影响。Pi0.5及S3验收正在自动顺序继续，正式批仍暂停。

后续v2在Pi0.5相同输入回放处停止：原main先裁剪全部50步物理夹爪，再复制10步执行前缀；初版常驻后处理沿用了PAP只裁剪前10步的方式。首块差异仅在未执行尾部夹爪，最大0.007821rad；六臂关节和执行前10步差异0。原冷启动`gazebo_pi05_workspace50_20260908_195247_ep0001_seed1`为有效success。不是新模型能力变化或归一化统计变动，不能以放宽阈值放行。

已作单点合同修复：`action_contract.py`对Pi0.5裁剪全50步、PAP保持原前10步，归一化预测与RTC leftover不变。9项单元测试通过；保存的首块应用修复后与原main物理50步差异0。新增冻结版`resident_online_canary_20260908_v3_gripper_contract`，只续做Pi0.5/S3；复用有效冷启动记录，先检查原源码和场景/执行合同，再逐块回放全部保存观测，覆盖完整RTC链，不重复生成已有冷启动成功轨迹。通过后分别做两条常驻Gazebo闭环；仍不混入正式成绩，不自动切换正式批。GPU复测和新在线结果待产出。

## 6. 常驻验收完成与正式启用

**最终结果（覆盖本节下方运行中状态）：45条正常结束，completion exit_code=0。** Pi0.5=6/15成功、8timeout、1控制器容差失败；S3=0/15、12timeout、3容差失败；S4=4/15、8timeout、3容差失败。五位置×三seed×三模型完整唯一、汇总与原始JSON一致、结束时源码指纹未变。继承14条＋读取修复后31条，后者无新工程无效；原无效条保留在v1、不计成功率。S4四次成功均曾观测完全坐底，Pi0.5两次，但旧成功终止可截断后续坐底过程，不能直接据此比较精度；均未验证释放稳定。7次控制器目标容差失败需另查指令/实测，不能用当前valid判定声称全部工程问题解决。详细位置表、来源和结论已写入[总报告最新补充](PAP_MoE推进顺序与实验结论_简版_20260907.md)。最终证据`artifacts/paired_comparison_20260908_resident_v2_stable_read/{summary.json,results.json,continuation.json}`。GPU已释放，旧暂停批不恢复。

监控后续节点（30/45）：Pi0.5完成6/15成功，S3完成0/15成功（12timeout＋3控制器目标容差失败），累计30条均通过当前工程有效性检查；S4已自动进入加载，尚不能报告三模型最终对比。S3三次容差失败集中在0001/0021/0041的seed1，原始分类unknown_failure保留；日志显示arm_error约0、夹爪0.048–0.065rad，需后续检查指令/实测轨迹，不能据此认定单一根因。S3多数在peg上方升降，部分在约70–90秒后抓取抬升，但未及时完成运输；这是闭环现象而非已证明的训练原因。原通信中断修复后尚未新增工程无效条目。

续跑在线节点：v2的Pi0.5/0041/seed2已完整运行至120秒，正常任务timeout、工程有效，未再出现paired超时；桥接超过原中断的sequence=50继续输出，本次未触发读取重试分支。竞争恢复由故障注入测试覆盖，不能称这条在线测试已经复现并消除了底层竞争。继承14条＋补测1条后的Pi0.5为6/15成功、8timeout、1控制器目标容差失败；原工程无效记录单独保留、不进分母。程序已自动转入S3权重加载，之后S4。尚无本批PAP对比结论。

> 后续状态更正（2026-09-08晚）：resident_v1在Pi0.5/0041/seed2保护停止。此前14条有效：6成功、7超时、1控制器目标容差失败；第15条是工程无效，不可统计为任务失败。S3/S4尚未进入。直接证据为该条`inference.log`在完成sequence=50后报`Observation changed while loading; refusing action`，ROS继而报paired timeout；最后正常推理219.7ms，源码哈希全相同。没有保存发生变化的两次ID，因此底层是否rename/ctime竞争尚不能定论。
>
> 已修复桥接的失败处理：仅模型调用前，最多0.25秒内重读一致快照，丢弃混合读取，不生成动作、不推进RTC/序号；device/inode/mtime未变但ctime变化不重复触发；稳定坏输入或持续不稳定仍终止，模型调用后ID变化仍拒发。未改backend、动作、统计、权重、ROS控制或任务合同。稳定/竞争/损坏/超时/续跑证据校验等22项测试通过。新只读运行`artifacts/paired_comparison_20260908_resident_v2_stable_read/`继承原14条全部有效结果（含失败），补测唯一工程无效键，再运行S3/S4各15条；原记录不覆盖，continuation记录源批和排除项。报告必须标注跨读取修复版本续跑，不能称作45次全部重测或证明两版成功率等价。在线修复有效性继续由实际运行检验。

v3验收正常结束（completion exit_code=0）。这次没有停在差异定位：完成Pi0.5限幅修复、全保存轨迹回放、两条在线复用及S3完整验收。数据和权重未改变。

| 模型/验收场景 | 原main相同观测回放 | 原冷启动 | 常驻第1条 | 常驻第2条 | 后续条重置 |
|---|---|---|---|---|---|
| Pi0.5 / 0001 seed1 | 21块，物理动作最大5.96e-8rad | 成功 | 成功 | 成功 | 0.814/0.788s |
| S3 / 0001 seed0 | 94块，动作与路由最大差异0 | 超时 | 超时 | 超时 | 0.863/0.857s |
| S4 / 0031 seed2 | 前2块，动作与路由最大差异0 | 超时 | OOD停止 | 成功，约80.003mm | 0.863/0.854s |

所有上述有效验收条目无工程异常，未验证释放稳定。每组两条常驻使用相同worker PID、不同session token。Pi0.5最大桥接延迟约223ms，S3最高约450ms，S4最高约305ms，均未触发paired超时。源/权重和动作合同检查加上11项轻量测试通过。不同组验收场景/种子不同，表格不是模型优劣比较；S4冷/热结果不同也说明有限验收不能证明成功概率相等。v1无效条、v2夹爪格式差异均保留，不改写旧成绩。

工程审阅后启用新正式批`artifacts/paired_comparison_20260908_resident_v1/`，由`formal_batch.py`创建独立只读代码快照，重新汇总三模型×五位置×三种子的45次结果。三模型统一常驻，每条重启Gazebo；checkpoint、数据、50/10、RTC、0.13rad限制、120s时限、5s控制器宽限、成功/几何判定不变，seed0录视频。旧冷启动批留存并保持暂停，不混入成绩；多做已完成位置是为了避免冷/热运行方式与模型身份混杂，不是重训或删除有效数据。每条冻结校验，工程无效停机，不自动重试。

新批输出`status.json`/`results.jsonl`/`batch.log`及最终`summary.json`。此处仅记录启动，不预填最终成功率。常驻省掉反复加载模型已实测，但正式整批加速比待相同统计口径的耗时结果，不能拿不同终止时长直接比较。
