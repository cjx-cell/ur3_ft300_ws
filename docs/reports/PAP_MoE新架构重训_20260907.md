# 新架构重训与评估（2026-09-07）

## Material Passport

- Origin Skill：academic-research-suite / experiment-agent，run。
- Verification Status：UNVERIFIED（实验执行中，尚无新成功率）。

## 固定合同

- 起点：统计正确的Pi0.5 Workspace50 30k；不继承9月4日至6日错误统计的PAP权重。
- 数据：`lerobot_v3_workspace50_v10_full_clean_global_stats_v1`，50条14418帧，不增加恢复数据。
- 结构：`action_input_tokens_v2`＋`physics_gate_v2`；没有Forecaster、草稿动作、subtask或进度头。
- 物理条件在动作Transformer之前融合；无动作基线锚定、无负载均衡、无条件残差限幅。视觉/历史无效token掩码开启。
- VLM冻结；第一阶段四专家及完整动作专家联合训练。物理/动作学习率比例1:1，峰值2.5e-5，batch 1。E1保留新架构默认early_sum，不继承尚无可靠收益的C组late_sum。
- 保留新版视觉历史与表征监督；因此不是仅改变融合位置的单变量实验。

## 自动队列

1. 真实50步路由条件下联合训练物理专家与动作专家30k，保存间隔3k；结束做oracle开环，不将未训练Gate用于部署评估。
2. 单PhysicsGate标签监督15k，保存并做预测路由开环、五位置×三seed闭环。
3. 冻结专家和动作主体，用动作损失＋路由正则校准门控栈15k，再做同口径开环、闭环。现有实现的门控栈包含共享力/状态/质量编码器，不应称为仅更新最后一个Gate线性头。
4. Pi0.5同锚点开环和同执行合同闭环复测，避免将旧力滤波条件下的历史7/15当作本轮配对结果。

开环：五位置、每物理类别最多8锚点、3噪声seed，记录flow MSE（不是完整解码动作误差）。闭环：50预测／10执行、seed0/1/2，seed0录视频。使用固定最终步数，不按结果挑检查点。PAP有额外训练预算和力输入，结果不能单独归因于MoE结构。

## 启动前修复与校验

- 在线中值力窗口每次都推进，匹配采集v6；增加实际源码语句的持续40N与单次400N脉冲回归测试。
- 本次定向测试：90 passed（包括错误统计即使前后自洽也必须被拒绝的回归测试）。
- 新训练入口强制单Gate；实际processor必须与全帧重算统计一致，失败就中止。训练内保存`verified_processors/verified_contract.json`，不能以“归一化类型相同”替代数值验证。
- 14个错误统计训练目录移至`.cleanup_trash/20260907_invalid_statistics/`，可恢复；没有释放磁盘空间。逐检查点比较及位置见该目录`moves.jsonl`。不同数据集的更早模型、统计正确的正式历史对照未动。

## 运行入口

- 服务：`pap-corrected-20260907.service`（用户systemd服务；独立于本次对话）。
- 队列：`scripts/run_corrected_pap_pipeline.py`；任何训练异常停止，不自动重试。
- 状态：[status.json](/home/ubuntu/ur3_ft300_ws/artifacts/pap_corrected_pipeline_20260907_000738/status.json)
- 首轮日志：[expert_action_joint.log](/home/ubuntu/ur3_ft300_ws/artifacts/pap_corrected_pipeline_20260907_000738/expert_action_joint.log)
- 各任务设超时上限；状态文件记录当前子进程、命令、已完成任务和异常。尚未完成新的Gazebo现场验证，不能声称全流程已通过。

00:11启动验收：已超过100步，实际训练processor校验通过，最大核验差约9.4e-8；可训练参数455,104,947。训练与专家辅助损失正常输出，尚不足以判断收敛或闭环收益。

09:06恢复记录：30k训练已在02:51正常结束并保存；随后外层shell返回127，阻断评估，并非训练崩溃。核验最终权重文件可读、架构正确、无Forecaster权重、六项状态/动作/历史分位数匹配后，从开环评估恢复，不重复训练。新服务为`pap-corrected-resume-20260907.service`，沿用同一结果目录，旧失败状态另存。后续训练使用独立runner快照，避免原脚本运行期间编辑导致shell读偏移。报告回写服务已重新启动。

后续监控记录：开环脚本在第一个位置计算完成后因缺少`baseline_action_loss`退出。新版输入融合不暴露该旧式诊断字段；按评估器接口要求增加`--mask all_zero`，显式计算同检查点关闭物理条件的对照。保留原失败日志，重新评估；不改权重、full指标、采样或闭环合同。该all_zero不是独立Pi0.5。当前执行服务为`pap-corrected-evalfix-20260907.service`，报告服务为`pap-corrected-report-evalfix-20260907.service`。

## 12:18 环境修复后重新评估

阶段3原批次15次中，仅2次进入模型执行（均未成功），13次轨迹控制服务不可用，初始化中止；不能报告为有效0/15。阶段4原批次也出现相同初始化问题。已停止旧队列，检查点不变、不重训。

发现旧启动器只查话题名，未查活跃控制器；退出未等待Gazebo进程组清理，确认存在残留服务器。DDS共享内存错误同时出现，不能仅据该日志认定唯一根因。

修复：每试验独立Gazebo transport partition、统一隔离ROS domain 77；执行前要求实时相机/关节消息、推进的clock、三个active控制器和轨迹Action服务；退出等待并清理所属进程组；文件通信增加独占锁；初始化无效明确标记且立即停止批次，不再算模型失败。未修改物理参数、模型、阈值或动作合同。所有模型统一使用新基础设施条件复测。

连续两次启动/退出自检通过，控制器和实时消息均正常，未发现Gazebo残留。新服务`pap-corrected-reeval-20260907.service`从阶段3、阶段4到Pi0.5各重评15次，并复测Pi0.5开环；结果保存到新的`artifacts/pap_corrected_reeval_*`，自动更新简版报告。尚需首个完整动作试验证明运行链路正常。

12:22首个重评试验已进入真实执行并结束，`evaluation_valid=true`，但仍因动作跟踪目标超时失败（末端日志arm_error约0.199rad），不是初始化失败。新批次目录`artifacts/pap_corrected_reeval_20260907_121751`，已自动进入第二次试验。环境启动修复不代表模型闭环问题已经解决。

## 14:28 启动检测收口与断点续评

12:17批次完成4次有效阶段3试验，成功0次；第5次（episode 11、seed 1）Gazebo在控制器服务响应时抛出`cannot publish data`并中止，属于无效初始化，不加入成功率分母。根因尚未完全证明。

检查发现就绪探针的控制器服务查询随消息回调频繁触发，且可能携带未完成请求退出。现限制查询为1Hz，控制器齐备后停止查询，成功退出时不允许存在未完成请求。不更换DDS传输方式，不改模型、归一化、物理参数及50/10执行合同。修复后连续6次启动自检均通过（14:24:33至14:25:11的启动目录）；这仅验证启动，不代表模型成功。

续评服务：`pap-corrected-continue-20260907.service`。结果目录：`artifacts/pap_corrected_reeval_20260907_142810`。只继承4条有效记录，并校验检查点及episode/seed唯一性；旧批次所有尝试另行留存。接续缺失的阶段3试验，然后阶段4和Pi0.5，最后Pi0.5同口径开环评估。任一基础设施失败仍停止并记录，不盲目重试。简版报告自动按有效试验回写。

14:29更正：上述续评的jq变量误用保留字`label`，跳过判断编译失败，误进入首条已完成试验；发现后立即停止，未采用该次重复试验。改名为`eval_label_value`并让查询编译错误直接停止，完成语法和已有记录匹配验证。实际续评服务为`pap-corrected-continue-v2-20260907.service`，目录`artifacts/pap_corrected_reeval_20260907_142914`。日志确认跳过全部4条已有记录，从episode 11、seed 1开始，现场就绪检查通过。

14:32续评首条完成：episode 11、seed 1加载权重成功、生成3个动作块后，轨迹控制器以`goal_time_tolerance`超时中止，arm_error约0.1296rad。并非初始化失败，也不能据此单独认定网络或控制器是根因。当前阶段3累计5条有效执行、0成功，尚未完成15条；队列已进入episode 11、seed 2。后续阶段4和Pi0.5同合同对比仍是判断模型差异的必要证据。
